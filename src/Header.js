import React from 'react';
import Card from 'react-bootstrap/Card'
export const Header_p = () => {

    return ( <
        div >
        <
        Card body > < h2 className = "justify-content-end" > Expense Tracker < /h2>  < /Card > < /
        div >
    )
}